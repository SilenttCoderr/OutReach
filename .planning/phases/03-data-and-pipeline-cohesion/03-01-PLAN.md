---
phase: 03-data-and-pipeline-cohesion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/application/campaign_service.py
  - src/api/routes_campaigns.py
  - src/tracker.py
  - tests/test_api.py
autonomous: true
requirements:
  - ARCH-04
must_haves:
  truths:
    - One canonical send-tracking source is used for pipeline behavior.
    - Legacy tracker path is isolated from primary operational state.
    - Clear/reset behavior aligns with DB-backed truth used by stats/history.
  artifacts:
    - path: src/application/campaign_service.py
      provides: canonicalized service-level state transitions and clear/reset behavior
    - path: src/api/routes_campaigns.py
      provides: stable API surface mapped to canonicalized service behavior
    - path: src/tracker.py
      provides: explicit legacy compatibility status (non-canonical)
  key_links:
    - from: src/api/routes_campaigns.py
      to: src/application/campaign_service.py
      via: clear-tracking and campaign workflow orchestration
      pattern: clear_tracking|draft|send
---

<objective>
Converge tracking/storage truth to DB-backed models and isolate legacy tracker behavior so pipeline status is coherent and deterministic.
</objective>

<context>
@.planning/phases/03-data-and-pipeline-cohesion/03-CONTEXT.md
@.planning/phases/03-data-and-pipeline-cohesion/03-RESEARCH.md
@src/application/campaign_service.py
@src/api/routes_campaigns.py
@src/models.py
@src/tracker.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonicalize clear/reset tracking behavior to DB-backed source</name>
  <files>src/application/campaign_service.py, src/api/routes_campaigns.py</files>
  <action>Replace legacy file-based clear behavior with DB-consistent reset logic that matches stats/history data source while preserving route contract semantics.</action>
  <verify>
    <automated>py -3 -m py_compile src/application/campaign_service.py src/api/routes_campaigns.py</automated>
  </verify>
  <done>Clear/reset no longer depends on `data/tracking.json` as operational truth.</done>
</task>

<task type="auto">
  <name>Task 2: Isolate legacy tracker module and document non-canonical role</name>
  <files>src/tracker.py</files>
  <action>Mark tracker behavior as legacy compatibility only and ensure no new primary-flow dependencies are introduced.</action>
  <verify>
    <automated>py -3 -m py_compile src/tracker.py</automated>
  </verify>
  <done>Legacy tracker is explicitly non-authoritative and isolated from primary pipeline flow.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression checks for canonical clear/reset and history consistency</name>
  <files>tests/test_api.py</files>
  <action>Add or adjust API tests to verify clear/reset and history/stat endpoints remain contract-compatible under canonical DB behavior.</action>
  <verify>
    <automated>py -3 -m pytest tests/test_api.py -k "history or stats or clear"</automated>
  </verify>
  <done>Regression checks cover canonicalized clear/reset + stats/history contract stability.</done>
</task>

</tasks>

<verification>
Run Python compile checks and focused API tests for stats/history/clear behavior.
</verification>

<success_criteria>
- Legacy file tracker is no longer operational source-of-truth.
- Clear/reset behavior aligns with DB-backed campaign visibility.
- Contract stability is covered by regression tests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-and-pipeline-cohesion/03-01-SUMMARY.md`
</output>
