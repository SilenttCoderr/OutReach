---
phase: 03-data-and-pipeline-cohesion
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/application/campaign_service.py
  - src/api/routes_campaigns.py
  - docs/ARCHITECTURE_CONTRACT.md
  - docs/DEPLOYMENT_CONTRACT.md
  - tests/test_api.py
autonomous: true
requirements:
  - ARCH-04
must_haves:
  truths:
    - Send pipeline transitions are deterministic for single-send and batch-send paths.
    - Failures are persisted with queryable state, not only console output.
    - Migration notes clearly document canonical state model and transition contracts.
  artifacts:
    - path: src/application/campaign_service.py
      provides: deterministic transition handling across single and batch send flows
    - path: tests/test_api.py
      provides: regression coverage for transition outcomes
    - path: docs/ARCHITECTURE_CONTRACT.md
      provides: explicit canonical tracking model documentation
  key_links:
    - from: src/application/campaign_service.py
      to: src/models.py
      via: status transitions for EmailLog and Contact
      pattern: draft|sent|failed|pending
---

<objective>
Harden send pipeline transition determinism, persist failure outcomes, and codify canonical model assumptions in architecture/deployment docs.
</objective>

<context>
@.planning/phases/03-data-and-pipeline-cohesion/03-CONTEXT.md
@.planning/phases/03-data-and-pipeline-cohesion/03-RESEARCH.md
@.planning/phases/03-data-and-pipeline-cohesion/03-01-SUMMARY.md
@src/application/campaign_service.py
@src/models.py
@docs/ARCHITECTURE_CONTRACT.md
@docs/DEPLOYMENT_CONTRACT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce deterministic status transitions in single-send and batch-send paths</name>
  <files>src/application/campaign_service.py, src/api/routes_campaigns.py</files>
  <action>Unify transition updates so `EmailLog` and corresponding contact statuses are consistently updated for draft/sent/failed outcomes in both single and batch sends.</action>
  <verify>
    <automated>py -3 -m py_compile src/application/campaign_service.py src/api/routes_campaigns.py</automated>
  </verify>
  <done>Single and batch send paths produce consistent and deterministic transition states.</done>
</task>

<task type="auto">
  <name>Task 2: Persist failure outcomes and expose them through existing stats/history paths</name>
  <files>src/application/campaign_service.py, tests/test_api.py</files>
  <action>Persist failure states in DB and add tests that validate failed transitions are reflected by existing API contracts.</action>
  <verify>
    <automated>py -3 -m pytest tests/test_api.py -k "send or draft or failed or history or stats"</automated>
  </verify>
  <done>Failures are queryable through canonical model and verified by regression tests.</done>
</task>

<task type="auto">
  <name>Task 3: Document canonical tracking and transition model for future phases</name>
  <files>docs/ARCHITECTURE_CONTRACT.md, docs/DEPLOYMENT_CONTRACT.md</files>
  <action>Add concise documentation for canonical tracking source, allowed transitions, and operational reset semantics.
</action>
  <verify>
    <automated>py -3 -m pytest tests/test_api.py -k "history or stats"</automated>
  </verify>
  <done>Architecture/deployment docs explicitly describe Phase 3 canonical state model and transition contract.</done>
</task>

</tasks>

<verification>
Compile campaign routes/services and run focused regression tests for draft/send/failure/stats/history behavior.
</verification>

<success_criteria>
- Pipeline state transitions are deterministic and canonical.
- Failure states are persisted and visible without log scraping.
- Canonical model and transition expectations are documented for downstream phases.
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-and-pipeline-cohesion/03-02-SUMMARY.md`
</output>
