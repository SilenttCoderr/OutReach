---
phase: 01-baseline-and-deployment-contracts
plan: 03
type: execute
wave: 1
depends_on:
  - 02
files_modified:
  - docs/DEPLOYMENT_CONTRACT.md
  - web/next.config.ts
  - vercel.json
  - .gitignore
autonomous: true
requirements:
  - DEPL-02
  - DEPL-04
user_setup: []
must_haves:
  truths:
    - Vercel/Render environment mapping is explicit and repeatable.
    - Dev/preview/prod API URL resolution behavior is documented and deterministic.
  artifacts:
    - docs/DEPLOYMENT_CONTRACT.md
  key_links:
    - docs/DEPLOYMENT_CONTRACT.md references web/next.config.ts and vercel.json as authoritative routing/deploy inputs.
---

<objective>
Define and align deployment contracts across frontend and backend surfaces.

Purpose: reduce misconfiguration drift between environments.
Output: deployment contract doc + config alignment updates.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-baseline-and-deployment-contracts/01-CONTEXT.md
@web/next.config.ts
@vercel.json
@docs/ONBOARDING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author deployment contract matrix</name>
  <files>docs/DEPLOYMENT_CONTRACT.md</files>
  <read_first>web/next.config.ts, vercel.json, docs/ONBOARDING.md</read_first>
  <action>Create docs/DEPLOYMENT_CONTRACT.md with sections "Environment Matrix", "API URL Resolution", "Required Variables by Platform", and "Rollback Checklist". Include explicit rows for local, Vercel preview, Vercel production, and Render production.</action>
  <acceptance_criteria>docs/DEPLOYMENT_CONTRACT.md contains headings "## Environment Matrix", "## API URL Resolution", "## Rollback Checklist", and includes the strings "Vercel preview" and "Render production".</acceptance_criteria>
  <verify>
    <automated>grep -n "Environment Matrix\|API URL Resolution\|Rollback Checklist\|Vercel preview\|Render production" docs/DEPLOYMENT_CONTRACT.md</automated>
  </verify>
  <done>Deployment contract doc exists with explicit environment mapping.</done>
</task>

<task type="auto">
  <name>Task 2: Align config hygiene and ignore rules</name>
  <files>web/next.config.ts, vercel.json, .gitignore</files>
  <read_first>web/next.config.ts, vercel.json, .gitignore, docs/DEPLOYMENT_CONTRACT.md</read_first>
  <action>Update web/next.config.ts and vercel.json comments/settings to match deployment contract terminology, and ensure .gitignore includes generated/runtime artifacts (`web/.next/`, `.venv/`, `uploads/`, `logs/`, `cold_outreach.db`) if missing.</action>
  <acceptance_criteria>.gitignore contains web/.next/, .venv/, uploads/, logs/, and cold_outreach.db, and docs/DEPLOYMENT_CONTRACT.md references both web/next.config.ts and vercel.json.</acceptance_criteria>
  <verify>
    <automated>grep -n "web/.next/\|.venv/\|uploads/\|logs/\|cold_outreach.db" .gitignore ; grep -n "web/next.config.ts\|vercel.json" docs/DEPLOYMENT_CONTRACT.md</automated>
  </verify>
  <done>Config and ignore hygiene align with deployment contract.</done>
</task>

</tasks>

<verification>
Run frontend build and backend health check in local dev with documented environment values.
</verification>

<success_criteria>
- Environment matrix exists and is actionable for operators.
- Config/hygiene alignment reduces accidental deployment drift.
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-and-deployment-contracts/01-03-SUMMARY.md`
</output>
