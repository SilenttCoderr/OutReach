---
phase: 01-baseline-and-deployment-contracts
plan: 02
type: execute
wave: 1
depends_on:
  - 01
files_modified:
  - src/config.py
  - app.py
  - requirements.txt
  - tests/test_startup_preflight.py
autonomous: true
requirements:
  - DEPL-01
  - QUAL-03
user_setup: []
must_haves:
  truths:
    - Backend startup fails early with actionable errors when critical env config is missing.
    - Known fragile dependency combinations are checked and guarded.
  artifacts:
    - src/config.py
    - tests/test_startup_preflight.py
  key_links:
    - app.py startup imports and runs preflight validation before serving traffic.
---

<objective>
Implement startup preflight checks for deployment safety and dependency compatibility.

Purpose: prevent runtime surprises after Render deploy.
Output: preflight module + startup hook + tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-baseline-and-deployment-contracts/01-CONTEXT.md
@.env.example
@requirements.txt
@app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build preflight validation module</name>
  <files>src/config.py</files>
  <read_first>.env.example, requirements.txt, app.py</read_first>
  <action>Create src/config.py exposing validate_startup_configuration() that checks required keys for production-safe paths (`SESSION_SECRET`, `JWT_SECRET`, `SECRET_KEY`, `FRONTEND_URL`) and validates bcrypt compatibility with passlib assumptions. Return structured error messages and raise ValueError on invalid configuration.</action>
  <acceptance_criteria>src/config.py contains function name validate_startup_configuration and includes the exact strings SESSION_SECRET, JWT_SECRET, SECRET_KEY, FRONTEND_URL, and bcrypt.</acceptance_criteria>
  <verify>
    <automated>grep -n "validate_startup_configuration\|SESSION_SECRET\|JWT_SECRET\|SECRET_KEY\|FRONTEND_URL\|bcrypt" src/config.py</automated>
  </verify>
  <done>Preflight validator exists with explicit required-key and dependency checks.</done>
</task>

<task type="auto">
  <name>Task 2: Wire preflight into startup and add tests</name>
  <files>app.py, tests/test_startup_preflight.py</files>
  <read_first>app.py, src/config.py, tests/test_api.py</read_first>
  <action>Import and run validate_startup_configuration() during startup path before serving requests in app.py. Add tests/test_startup_preflight.py covering success case and missing-env failure case using monkeypatch environment setup.</action>
  <acceptance_criteria>app.py contains validate_startup_configuration call and tests/test_startup_preflight.py contains at least two test functions with names containing preflight.</acceptance_criteria>
  <verify>
    <automated>grep -n "validate_startup_configuration" app.py ; grep -n "def test_.*preflight" tests/test_startup_preflight.py</automated>
  </verify>
  <done>Startup validation is executed and regression-tested.</done>
</task>

</tasks>

<verification>
Run pytest for preflight tests and verify startup still returns healthy for valid env.
</verification>

<success_criteria>
- Startup fails predictably for invalid config and passes for valid config.
- Dependency compatibility guardrails are codified and testable.
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-and-deployment-contracts/01-02-SUMMARY.md`
</output>
