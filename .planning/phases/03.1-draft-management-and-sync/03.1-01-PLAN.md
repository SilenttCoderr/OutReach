---
phase: 03.1-draft-management-and-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["src/gmail_client.py", "src/infrastructure/gmail_adapter.py", "src/application/campaign_service.py", "src/api/routes_campaigns.py"]
autonomous: true
requirements: [SYNC-01, SYNC-02]

must_haves:
  truths:
    - User can edit a generated draft.
    - User can delete a generated draft.
    - Gmail draft deletions sync proactively so users cannot send deleted drafts.
  artifacts:
    - path: src/gmail_client.py
      provides: Update, Get, Delete functions for drafts using Google SDK.
    - path: src/api/routes_campaigns.py
      provides: Exported draft update, delete, and sync API endpoints.
  key_links:
    - from: src/api/routes_campaigns.py
      to: src/application/campaign_service.py 
      via: HTTP request to fastAPI mapping Draft operations.
---

<objective>
To implement complete draft lifecycle operations (read, delete, update) against Gmail's API and safely link the state tracking back into our `EmailLog` database.
Purpose: Draft edit and deletion are necessary since AI outputs are not always perfect and users will sometimes make direct adjustments via the Gmail Web Client, causing a desync.
Output: Working endpoint logic serving full Draft CRUD.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Gmail Client Methods</name>
  <files>src/gmail_client.py, src/infrastructure/gmail_adapter.py</files>
  <action>
    Update GmailClient to include `update_draft`, `delete_draft`, and `get_draft` methods calling the `self.service.users().drafts().*` endpoints. Modify the adapter so it exposes these commands identically across architecture boundaries.
  </action>
  <verify>
    <automated>pytest tests/test_api.py</automated>
  </verify>
  <done>Raw Gmail functions available.</done>
</task>

<task type="auto">
  <name>Task 2: Build Sync Logic in Campaign Service</name>
  <files>src/application/campaign_service.py</files>
  <action>
    Add methods in `campaign_service.py`: `update_draft`, `delete_draft`, and a sync loop injected during `get_draft_logs` logic fetching all records with status `draft`. The sync checks `gmail.get_draft` – if it yields a `404`, update local `EmailLog`.
  </action>
  <verify>
    <automated>pytest tests/test_api.py</automated>
  </verify>
  <done>Service layer propagates update, sync, and delete successfully to Gmail adapter and EmailLog.</done>
</task>

<task type="auto">
  <name>Task 3: Backend Delete/Update Routes</name>
  <files>src/api/routes_campaigns.py</files>
  <action>
    Add FastAPI endpoints for HTTP:
    * PUT /api/draft/{id} (payload `{subject, body}`)
    * DELETE /api/draft/{id}
    * POST /api/drafts/sync (optional: for actively triggering syncs of UI states)
  </action>
  <verify>
    <automated>pytest tests/test_api.py</automated>
  </verify>
  <done>APIs return correct statuses for successful drafts management execution.</done>
</task>

</tasks>

<verification>
Start the backend and run curl or Postman against the endpoints ensuring no 500s or mismatched state between Gmail actual drafts and Response JSON payload.
</verification>

<success_criteria>
Gmail API successfully bridges updates and deleted logic without unhandled edge cases disrupting operations.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-draft-management-and-sync/03.1-01-SUMMARY.md`
</output>