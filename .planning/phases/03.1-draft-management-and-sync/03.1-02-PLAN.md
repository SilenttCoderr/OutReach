---
phase: 03.1-draft-management-and-sync
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: ["web/src/services/api.ts", "web/src/app/dashboard/drafts/page.tsx"]
autonomous: false
requirements: [SYNC-03]

must_haves:
  truths:
    - User can edit a drafted email right from the Drafts UI screen.
    - User can delete a draft in the Drafts UI.
    - UI correctly pulls the exact synchronized state.
  artifacts:
    - path: web/src/app/dashboard/drafts/page.tsx
      provides: Data Table with extra UI action buttons.
  key_links:
    - from: web/src/app/dashboard/drafts/page.tsx
      to: web/src/services/api.ts
      via: HTTP request functions for updating/deleting arrays.
---

<objective>
To wire the new backend Draft sync APIs into the React UI. Adding edit forms, delete dialogs, and a manual sync trigger button to keep Draft status parallel with literal Gmail state.
Purpose: Allow user review and manual correction of LLM output without leaving the OutreachPro interface or becoming desynced.
Output: UI table supports complex Draft workflows cleanly.
</objective>

<context>
@web/src/services/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend API Client</name>
  <files>web/src/services/api.ts</files>
  <action>
    Add `updateDraft`, `deleteDraft`, and `syncDrafts` functions mapped to their respective API paths. Ensure interface schemas are clean and type-safe.
  </action>
  <verify>
    <automated>cd web && npm run lint</automated>
  </verify>
  <done>Frontend api module exposes complete Draft CRUD operations.</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>UI Action additions for Draft Management</what-built>
  <how-to-verify>
    Modify `web/src/app/dashboard/drafts/page.tsx`. Provide Edit and Delete action controls next to each draft row. Clicking edit opens a simple modal allowing `subject` and `body` edits with a `Save` button hooked into the new API calls. Provide a manual "Sync with Gmail" refresh button atop the table. Test if edits correctly push via the UI locally on http://localhost:3000.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Start dev server and test clicking edit, modify UI text, save it, verify that loading indicator clears, and wait for refresh showing updated draft output in table.
</verification>

<success_criteria>
UI displays and interacts successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-draft-management-and-sync/03.1-02-SUMMARY.md`
</output>