---
phase: 02-backend-modularization-pass
plan: 02
type: execute
wave: 1
depends_on:
  - 01
files_modified:
  - src/application/campaign_service.py
  - src/application/contact_service.py
  - src/api/routes_campaigns.py
  - src/api/routes_contacts.py
autonomous: true
requirements:
  - ARCH-03
user_setup: []
must_haves:
  truths:
    - Route handlers delegate orchestration to application services.
    - Business workflow logic is no longer duplicated across endpoints.
  artifacts:
    - src/application/campaign_service.py
    - src/application/contact_service.py
  key_links:
    - API routers invoke application service methods for core operations.
---

<objective>
Introduce application service layer for extracted endpoint orchestration.

Purpose: isolate business workflows from transport layer and improve maintainability.
Output: service modules consumed by API router modules.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-backend-modularization-pass/02-CONTEXT.md
@docs/ARCHITECTURE_CONTRACT.md
@src/api/routes_campaigns.py
@src/api/routes_contacts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement campaign and contact application services</name>
  <files>src/application/campaign_service.py, src/application/contact_service.py</files>
  <read_first>docs/ARCHITECTURE_CONTRACT.md, app.py, src/models.py</read_first>
  <action>Create application service modules with explicit functions/classes for campaign draft/send orchestration and contact retrieval/filtering behavior currently embedded in route handlers.</action>
  <acceptance_criteria>src/application/campaign_service.py and src/application/contact_service.py exist and expose callable orchestration entry points used by routes.</acceptance_criteria>
  <verify>
    <automated>grep -n "def \|class " src/application/campaign_service.py src/application/contact_service.py</automated>
  </verify>
  <done>Application service layer exists for extracted workflows.</done>
</task>

<task type="auto">
  <name>Task 2: Replace route orchestration with service delegation</name>
  <files>src/api/routes_campaigns.py, src/api/routes_contacts.py</files>
  <read_first>src/application/campaign_service.py, src/application/contact_service.py, src/api/routes_campaigns.py, src/api/routes_contacts.py</read_first>
  <action>Refactor route handlers so they call application service functions for orchestration and keep route modules focused on request parsing, dependency injection, and response formatting.</action>
  <acceptance_criteria>Route files import from src/application modules and no longer contain multi-step orchestration blocks that directly coordinate multiple persistence/integration concerns.</acceptance_criteria>
  <verify>
    <automated>grep -n "from src.application\|import .*service" src/api/routes_campaigns.py src/api/routes_contacts.py</automated>
  </verify>
  <done>API layer is transport-focused and delegates orchestration to services.</done>
</task>

</tasks>

<verification>
Run API smoke tests and ensure response schemas remain unchanged for extracted endpoints.
</verification>

<success_criteria>
- Application services own core orchestration for extracted slices.
- API routers remain thin and framework-facing only.
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-modularization-pass/02-02-SUMMARY.md`
</output>
