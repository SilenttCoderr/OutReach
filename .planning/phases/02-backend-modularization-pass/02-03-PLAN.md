---
phase: 02-backend-modularization-pass
plan: 03
type: execute
wave: 2
depends_on:
  - 01
  - 02
files_modified:
  - src/infrastructure/gmail_adapter.py
  - src/infrastructure/stripe_adapter.py
  - src/application/campaign_service.py
  - src/application/billing_service.py
autonomous: true
requirements:
  - ARCH-02
  - ARCH-03
user_setup: []
must_haves:
  truths:
    - Application layer depends on adapter interfaces instead of provider SDK calls.
    - External provider calls are encapsulated in infrastructure adapter modules.
  artifacts:
    - src/infrastructure/gmail_adapter.py
    - src/infrastructure/stripe_adapter.py
    - src/application/billing_service.py
  key_links:
    - application services call infrastructure adapters, preserving behavior while reducing direct coupling.
---

<objective>
Introduce infrastructure adapters for external provider interactions used by modularized services.

Purpose: enforce architecture contract and reduce direct SDK coupling in upper layers.
Output: adapter modules and updated service wiring.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-backend-modularization-pass/02-CONTEXT.md
@docs/ARCHITECTURE_CONTRACT.md
@src/gmail_client.py
@src/stripe_routes.py
@src/application/campaign_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create provider adapter modules</name>
  <files>src/infrastructure/gmail_adapter.py, src/infrastructure/stripe_adapter.py</files>
  <read_first>src/gmail_client.py, src/stripe_routes.py, docs/ARCHITECTURE_CONTRACT.md</read_first>
  <action>Create adapter wrappers that expose focused operations needed by application services (gmail draft/send operations and stripe checkout/webhook fulfillment helpers), while encapsulating provider SDK details.</action>
  <acceptance_criteria>src/infrastructure/gmail_adapter.py and src/infrastructure/stripe_adapter.py exist with callable adapter methods/functions that do not expose raw route-layer concerns.</acceptance_criteria>
  <verify>
    <automated>grep -n "def \|class " src/infrastructure/gmail_adapter.py src/infrastructure/stripe_adapter.py</automated>
  </verify>
  <done>Infrastructure adapters exist for external providers used in modularized services.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire services to use adapters</name>
  <files>src/application/campaign_service.py, src/application/billing_service.py</files>
  <read_first>src/application/campaign_service.py, src/infrastructure/gmail_adapter.py, src/infrastructure/stripe_adapter.py</read_first>
  <action>Update application service modules to call adapter APIs instead of directly importing provider-heavy client implementations. Add billing_service abstraction if missing and route billing orchestration through it.</action>
  <acceptance_criteria>Application service files import from src/infrastructure adapters and avoid direct provider SDK imports in service-layer code.</acceptance_criteria>
  <verify>
    <automated>grep -n "from src.infrastructure" src/application/campaign_service.py src/application/billing_service.py</automated>
  </verify>
  <done>Service layer uses infrastructure adapters for provider interactions.</done>
</task>

</tasks>

<verification>
Run auth/billing/campaign smoke checks to ensure behavior parity after adapter introduction.
</verification>

<success_criteria>
- Provider interactions are adapterized for modular architecture.
- Application layer no longer directly depends on provider SDK details.
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-modularization-pass/02-03-SUMMARY.md`
</output>
