---
phase: 02-backend-modularization-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.py
  - src/api/routes_campaigns.py
  - src/api/routes_contacts.py
  - src/api/dependencies.py
autonomous: true
requirements:
  - ARCH-02
  - ARCH-03
user_setup: []
must_haves:
  truths:
    - app.py primarily composes middleware and routers.
    - campaign/contact endpoints are mounted via modular router modules.
  artifacts:
    - src/api/routes_campaigns.py
    - src/api/routes_contacts.py
    - src/api/dependencies.py
  key_links:
    - app.py includes extracted routers without endpoint contract changes.
---

<objective>
Extract campaign and contact routes into dedicated API modules.

Purpose: reduce backend entrypoint coupling and enable cleaner service extraction.
Output: modular routers wired through app composition root.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-backend-modularization-pass/02-CONTEXT.md
@docs/ARCHITECTURE_CONTRACT.md
@app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create API route modules and shared dependencies</name>
  <files>src/api/routes_campaigns.py, src/api/routes_contacts.py, src/api/dependencies.py</files>
  <read_first>app.py, src/models.py, src/database.py, docs/ARCHITECTURE_CONTRACT.md</read_first>
  <action>Create router modules for campaign-related and contact-related endpoints currently defined in app.py. Add shared dependency helpers in src/api/dependencies.py for common injected dependencies used by extracted routes.</action>
  <acceptance_criteria>src/api/routes_campaigns.py and src/api/routes_contacts.py each define `router = APIRouter(...)`, and src/api/dependencies.py exists with at least one dependency helper function.</acceptance_criteria>
  <verify>
    <automated>grep -n "router = APIRouter" src/api/routes_campaigns.py src/api/routes_contacts.py</automated>
  </verify>
  <done>Route modules exist with shared dependency helper module.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire app.py to include extracted routers</name>
  <files>app.py</files>
  <read_first>app.py, src/api/routes_campaigns.py, src/api/routes_contacts.py</read_first>
  <action>Replace in-file endpoint definitions for extracted campaign/contact routes with imports and include_router wiring in app.py, keeping existing URL paths and methods unchanged.</action>
  <acceptance_criteria>app.py includes imports from src/api/routes_campaigns.py and src/api/routes_contacts.py and mounts both routers using include_router with `/api` prefix compatibility preserved.</acceptance_criteria>
  <verify>
    <automated>grep -n "routes_campaigns\|routes_contacts\|include_router" app.py</automated>
  </verify>
  <done>app.py acts as composition root for extracted route modules.</done>
</task>

</tasks>

<verification>
Run endpoint smoke checks for `/api/stats`, `/api/contacts`, `/api/drafts` after extraction.
</verification>

<success_criteria>
- Route extraction is complete with no endpoint contract drift.
- app.py size/complexity is reduced for extracted slices.
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-modularization-pass/02-01-SUMMARY.md`
</output>
