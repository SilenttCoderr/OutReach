---
phase: 02.1-frontend-refinement-and-ship-readiness
plan: 06
subsystem: ui
tags: [nextjs, react, tailwindcss, design-tokens, linting]
requires:
  - phase: 02.1-frontend-refinement-and-ship-readiness-05
    provides: shared status/error primitives and stabilized dashboard state handling
provides:
  - explicit tokenized color and typography contract refinements in globals
  - hardcoded auth color and spacing outlier removal on reviewed target surfaces
  - executable ui:guard static check for style-token bypass regressions
affects: [frontend-refinement, release-gates, quality]
tech-stack:
  added: [node static guard script]
  patterns: [token-first styling, static class-pattern regression checks]
key-files:
  created: [web/scripts/ui-guard.mjs]
  modified: [web/src/app/globals.css, web/src/app/login/page.tsx, web/src/app/signup/page.tsx, web/src/app/auth/callback/page.tsx, web/src/app/dashboard/page.tsx, web/src/app/dashboard/profile/page.tsx, web/src/app/dashboard/templates/page.tsx, web/package.json]
key-decisions:
  - "Use token-driven provider button and status banner classes instead of page-local hardcoded auth colors."
  - "Enforce a narrow, explicit guard rule set tied to known UI review regressions for reliable CI gating."
patterns-established:
  - "Shared token-aligned provider button (`btn-provider`) for third-party auth entrypoints"
  - "`ui:guard` as a fast pre-merge check for forbidden utility-style regressions"
requirements-completed: [QUAL-01, QUAL-02]
duration: 9min
completed: 2026-04-07
---

# Phase 02.1 Plan 06: Visual-System Guardrails Summary

**Token-aligned auth and dashboard styling was consolidated and protected with an automated ui:guard command that blocks known color/spacing regressions.**

## Performance

- **Duration:** 9 min
- **Started:** 2026-04-06T23:34:00Z
- **Completed:** 2026-04-06T23:43:57Z
- **Tasks:** 3
- **Files modified:** 9

## Accomplishments

- Clarified global style contract with explicit semantic status mappings, constrained typography-scale tokens, and token-native utility helpers.
- Removed known hardcoded auth color and arbitrary spacing outliers from login, signup, callback, dashboard, profile, and templates surfaces.
- Added `ui:guard` automation plus npm script wiring so merge pipelines can fail fast on reviewed token-bypass anti-patterns.

## Task Commits

Each task was committed atomically:

1. **Task 1: Tighten tokenized visual contract for color and typography in globals** - `c9ff5b8` (refactor)
2. **Task 2: Remove known hardcoded-color and arbitrary-spacing outliers from target surfaces** - `fde0fe3` (refactor)
3. **Task 3: Add lightweight style guard command for token-bypass regressions** - `a1fed3d` (feat)

**Plan metadata:** recorded in `docs(02.1-06)` completion commit

## Files Created/Modified

- `web/src/app/globals.css` - expanded semantic color/typography token contract and reusable token-aligned utility classes.
- `web/src/app/login/page.tsx` - switched auth error rendering and provider button shell to tokenized styling.
- `web/src/app/signup/page.tsx` - aligned signup provider/error UI to shared tokenized patterns.
- `web/src/app/auth/callback/page.tsx` - updated callback loading view and suspense fallback to established token palette.
- `web/src/app/dashboard/page.tsx` - replaced reviewed arbitrary skeleton width outlier with scale utility.
- `web/src/app/dashboard/profile/page.tsx` - replaced reviewed arbitrary textarea sizing/line-height with scale-based classes.
- `web/src/app/dashboard/templates/page.tsx` - replaced reviewed arbitrary add-card min-height class.
- `web/scripts/ui-guard.mjs` - added configurable static checks for forbidden style patterns with line-level diagnostics.
- `web/package.json` - added runnable `ui:guard` command.

## Decisions Made

- Preserved Google brand icon fills, but removed hardcoded white/black/gray shell classes from auth provider buttons to keep page chrome tokenized.
- Guard checks are intentionally scoped to concrete regressions identified in the UI review, balancing enforcement with low false-positive risk.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- `eslint` warns that `globals.css` is ignored by file-level config when run directly; this was non-blocking and Task 1 verification remained warning-only with no lint errors.

## Known Stubs

- `web/src/app/dashboard/templates/page.tsx:86` contains "Template editor coming soon". This is an existing product placeholder and was not modified in scope of Plan 06.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 07 can now use `ui:guard` as part of release-gate verification.
- Styling contract is clearer for future frontend changes and less prone to token bypass drift.

## Self-Check: PASSED

- FOUND: `.planning/phases/02.1-frontend-refinement-and-ship-readiness/02.1-06-SUMMARY.md`
- FOUND: `c9ff5b8`
- FOUND: `fde0fe3`
- FOUND: `a1fed3d`
