---
phase: 02.1-frontend-refinement-and-ship-readiness
plan: 06
type: execute
wave: 4
depends_on:
  - 05
files_modified:
  - web/src/app/globals.css
  - web/src/app/login/page.tsx
  - web/src/app/signup/page.tsx
  - web/src/app/auth/callback/page.tsx
  - web/src/app/dashboard/page.tsx
  - web/src/app/dashboard/profile/page.tsx
  - web/src/app/dashboard/templates/page.tsx
  - web/scripts/ui-guard.mjs
  - web/package.json
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
user_setup: []
must_haves:
  truths:
    - Frontend styling relies on tokenized color/typography conventions rather than ad hoc utility color classes.
    - Known arbitrary spacing outliers from the UI review are normalized.
    - A repeatable automated guard catches obvious token-bypass regressions before merge.
  artifacts:
    - path: web/src/app/globals.css
      provides: constrained color and typography design-token contract
    - path: web/scripts/ui-guard.mjs
      provides: automated static checks for forbidden hardcoded style patterns
    - path: web/package.json
      provides: runnable `ui:guard` script for local and CI use
    - path: web/src/app/auth/callback/page.tsx
      provides: token-aligned auth callback styling
  key_links:
    - from: web/package.json
      to: web/scripts/ui-guard.mjs
      via: ui guard npm script
      pattern: ui:guard
    - from: web/src/app/login/page.tsx
      to: web/src/app/globals.css
      via: tokenized error state classes
      pattern: input|btn|text-error
    - from: web/src/app/dashboard/templates/page.tsx
      to: web/src/app/globals.css
      via: spacing/token normalization
      pattern: min-h
---

<objective>
Consolidate visual-system consistency and add lightweight enforcement against style-token regressions.

Purpose: resolve revamp-level color/typography/spacing drift identified in UI review and keep those fixes durable.
Output: updated tokenized styling contract plus executable `ui:guard` static check.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02.1-frontend-refinement-and-ship-readiness/02.1-CONTEXT.md
@.planning/phases/02.1-frontend-refinement-and-ship-readiness/02.1-RESEARCH.md
@.planning/phases/02.1-frontend-refinement-and-ship-readiness/02.1-UI-REVIEW.md
@web/src/app/globals.css
@web/src/app/login/page.tsx
@web/src/app/signup/page.tsx
@web/src/app/auth/callback/page.tsx
@web/src/app/dashboard/page.tsx
@web/src/app/dashboard/profile/page.tsx
@web/src/app/dashboard/templates/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tighten tokenized visual contract for color and typography in globals</name>
  <files>web/src/app/globals.css</files>
  <behavior>
    - Token contract remains compatible with existing utility classes.
    - Error/success/accent usage has clear semantic mappings.
    - Typography scale constraints are explicit and discoverable by contributors.
  </behavior>
  <action>Refine `globals.css` token comments/mappings and component utility classes to make preferred color and typography usage explicit per D-02 and D-03. Preserve existing look direction while reducing drift opportunities.</action>
  <verify>
    <automated>Set-Location web; npm.cmd run lint -- src/app/globals.css</automated>
  </verify>
  <done>Tokenized visual contract is explicit, coherent, and lint clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove known hardcoded-color and arbitrary-spacing outliers from target surfaces</name>
  <files>web/src/app/login/page.tsx, web/src/app/signup/page.tsx, web/src/app/auth/callback/page.tsx, web/src/app/dashboard/page.tsx, web/src/app/dashboard/profile/page.tsx, web/src/app/dashboard/templates/page.tsx</files>
  <behavior>
    - Auth pages no longer use hardcoded red utility classes for error banners.
    - Callback view aligns to established token palette.
    - Known arbitrary spacing classes from UI review are replaced with consistent scale choices.
  </behavior>
  <action>Migrate targeted files away from hardcoded utility-color and spacing outliers identified in UI review while preserving user behavior per D-05. Keep Google brand icon fills only where branding requires it.</action>
  <verify>
    <automated>Set-Location web; npm.cmd run lint -- src/app/login/page.tsx src/app/signup/page.tsx src/app/auth/callback/page.tsx src/app/dashboard/page.tsx src/app/dashboard/profile/page.tsx src/app/dashboard/templates/page.tsx</automated>
  </verify>
  <done>Target surfaces are token-aligned and free of the flagged outlier classes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add lightweight style guard command for token-bypass regressions</name>
  <files>web/scripts/ui-guard.mjs, web/package.json</files>
  <behavior>
    - Guard command scans key frontend files for forbidden hardcoded style patterns.
    - Script exits non-zero on violations so CI can gate merges.
    - Rule set is documented in-code and easy to adjust.
  </behavior>
  <action>Create `ui-guard.mjs` and package script (`ui:guard`) that checks for known anti-patterns from this review (hardcoded auth error colors and selected arbitrary spacing patterns) per D-04.</action>
  <verify>
    <automated>Set-Location web; npm.cmd run ui:guard; npm.cmd run build</automated>
  </verify>
  <done>`ui:guard` runs locally and fails on prohibited style patterns targeted by this revamp.</done>
</task>

</tasks>

<verification>
Run targeted lint plus `ui:guard` and full frontend build to validate style-system changes do not regress compilation.
</verification>

<success_criteria>
- Targeted hardcoded-color and spacing outliers from UI review are removed.
- Visual token conventions are clearer and easier to maintain.
- `ui:guard` adds repeatable regression protection for style-token violations.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-frontend-refinement-and-ship-readiness/02.1-06-SUMMARY.md`
</output>
