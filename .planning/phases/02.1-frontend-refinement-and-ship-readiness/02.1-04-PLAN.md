---
phase: 02.1-frontend-refinement-and-ship-readiness
plan: 04
type: execute
wave: 3
depends_on:
  - 02
  - 03
files_modified:
  - web/package.json
  - web/playwright.config.ts
  - web/tests/e2e/critical-flows.spec.ts
  - .github/workflows/ci.yml
  - docs/LAUNCH_CHECKLIST.md
  - .planning/phases/02.1-frontend-refinement-and-ship-readiness/02.1-UAT.md
autonomous: false
requirements:
  - DEPL-03
  - QUAL-01
  - QUAL-02
user_setup:
  - service: vercel
    why: Verify deployed frontend behavior before launch cutover.
    dashboard_config:
      - task: Confirm production deployment URL and latest successful deployment status.
        location: Vercel dashboard for the `web/` project
must_haves:
  truths:
    - Frontend release gates fail fast on lint/build/smoke regressions.
    - Critical user journeys have automated smoke coverage and executable commands.
    - Human UAT confirms release behavior on deployed environment before ship.
  artifacts:
    - path: web/tests/e2e/critical-flows.spec.ts
      provides: smoke checks for login/signup and dashboard critical paths
    - path: web/playwright.config.ts
      provides: deterministic e2e test configuration
    - path: .github/workflows/ci.yml
      provides: automated lint/build/smoke enforcement in CI
    - path: .planning/phases/02.1-frontend-refinement-and-ship-readiness/02.1-UAT.md
      provides: documented UAT outcomes and sign-off notes
  key_links:
    - from: .github/workflows/ci.yml
      to: web/package.json
      via: frontend quality scripts
      pattern: lint|build|test:e2e
    - from: web/tests/e2e/critical-flows.spec.ts
      to: web/src/app
      via: route-level smoke expectations
      pattern: /login|/signup|/dashboard
---

<objective>
Install release-safety gates and final verification for frontend ship readiness.

Purpose: ensure frontend polish work is protected by automation and validated through explicit UAT before launch.
Output: CI quality gates, smoke tests, and phase UAT sign-off artifact.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02.1-frontend-refinement-and-ship-readiness/02.1-CONTEXT.md
@docs/LAUNCH_CHECKLIST.md
@web/package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add frontend smoke suite for critical user journeys</name>
  <files>web/package.json, web/playwright.config.ts, web/tests/e2e/critical-flows.spec.ts</files>
  <behavior>
    - Automated smoke tests cover auth entry and dashboard access routes.
    - Test command is runnable in CI and local workflows.
    - Failures clearly indicate broken critical path behavior.
  </behavior>
  <action>Introduce Playwright-based smoke coverage for key flows (signup/login -> dashboard shell -> primary dashboard route access) and add a package script for repeatable execution per D-04.</action>
  <verify>
    <automated>Set-Location web; npm.cmd run test:e2e -- --reporter=line</automated>
  </verify>
  <done>Smoke test config and critical flow spec exist and execute via package script.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire frontend lint/build/smoke into CI and launch checklist verification</name>
  <files>.github/workflows/ci.yml, docs/LAUNCH_CHECKLIST.md</files>
  <behavior>
    - CI blocks merges when frontend lint/build/smoke checks fail.
    - Launch checklist includes explicit reference to automated frontend gate results.
    - Quality gates remain compatible with existing backend checks.
  </behavior>
  <action>Create or update CI workflow to run frontend lint/build/smoke checks and document the release gate expectation in launch checklist artifacts, aligned to DEPL-03 and QUAL-01.</action>
  <verify>
    <automated>Set-Location web; npm.cmd run lint; npm.cmd run build</automated>
  </verify>
  <done>CI and launch docs explicitly enforce and describe frontend quality gates required before release.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final UAT verification of polished frontend on deployed environment</name>
  <files>.planning/phases/02.1-frontend-refinement-and-ship-readiness/02.1-UAT.md</files>
  <action>Execute the final human verification pass on deployed frontend after automation gates are green, then record UAT findings and sign-off decision in `02.1-UAT.md` per D-04.</action>
  <what-built>Refined public and dashboard UI plus automated frontend quality gates from Plans 01-04.</what-built>
  <how-to-verify>
    1. Open deployed frontend URL and verify landing, pricing, docs, privacy, and terms page rendering on desktop and mobile viewport widths.
    2. Execute signup/login (email and Google where configured) and confirm dashboard landing without redirect loop.
    3. In dashboard, verify contacts upload/manual add, draft generation, draft send, and profile save flows.
    4. Confirm CI run for the branch shows passing frontend lint/build/smoke gates.
    5. Record pass/fail notes in `.planning/phases/02.1-frontend-refinement-and-ship-readiness/02.1-UAT.md`.
  </how-to-verify>
  <verify>
    <automated>Set-Location web; npm.cmd run lint; npm.cmd run build; npm.cmd run test:e2e -- --reporter=line</automated>
  </verify>
  <done>UAT notes are documented and user approval (or issue list) is explicitly captured before phase completion.</done>
  <resume-signal>Type "approved" if all checks pass, or provide issues to address.</resume-signal>
</task>

</tasks>

<verification>
Run frontend lint/build/smoke commands and complete human UAT on deployed environment before marking phase complete.
</verification>

<success_criteria>
- CI enforces frontend quality gates for lint/build/smoke checks.
- Critical frontend user journeys are covered by automated smoke tests.
- UAT sign-off is captured with clear pass/fail outcome and any residual issues.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-frontend-refinement-and-ship-readiness/02.1-04-SUMMARY.md`
</output>
