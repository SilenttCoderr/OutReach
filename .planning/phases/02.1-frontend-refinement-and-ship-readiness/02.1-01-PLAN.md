---
phase: 02.1-frontend-refinement-and-ship-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/services/api.ts
  - web/src/app/auth/callback/page.tsx
  - web/src/app/login/page.tsx
  - web/src/app/signup/page.tsx
  - web/src/app/pricing/page.tsx
  - web/next.config.ts
autonomous: true
requirements:
  - DEPL-02
  - QUAL-01
user_setup: []
must_haves:
  truths:
    - Frontend auth and checkout redirects behave consistently across local, preview, and production origins.
    - Frontend API helpers are lint-clean and type-safe for all authenticated requests.
    - Public auth and pricing flows surface stable user-facing errors instead of silent failures.
  artifacts:
    - path: web/src/services/api.ts
      provides: typed request helpers and centralized auth/error handling
    - path: web/src/app/auth/callback/page.tsx
      provides: stable OAuth callback completion behavior
    - path: web/src/app/login/page.tsx
      provides: deterministic email and Google sign-in routing
    - path: web/src/app/signup/page.tsx
      provides: deterministic email and Google sign-up routing
    - path: web/src/app/pricing/page.tsx
      provides: stable checkout handoff and auth fallback behavior
  key_links:
    - from: web/src/app/login/page.tsx
      to: web/src/services/api.ts
      via: loginWithEmail
      pattern: loginWithEmail
    - from: web/src/app/signup/page.tsx
      to: web/src/services/api.ts
      via: registerWithEmail
      pattern: registerWithEmail
    - from: web/src/app/pricing/page.tsx
      to: web/src/services/api.ts
      via: buyCredits
      pattern: buyCredits
---

<objective>
Harden the frontend API/session boundary and remove ship-blocking lint/type debt in auth and checkout surfaces.

Purpose: ensure environment-safe API origin handling and predictable auth/checkout behavior before broader UI polish.
Output: typed API helper layer and stabilized login/signup/callback/pricing behavior.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-frontend-refinement-and-ship-readiness/02.1-CONTEXT.md
@docs/LAUNCH_CHECKLIST.md
@web/src/services/api.ts
@web/src/app/auth/callback/page.tsx
@web/src/app/login/page.tsx
@web/src/app/signup/page.tsx
@web/src/app/pricing/page.tsx

<interfaces>
From web/src/services/api.ts:
```typescript
export interface AuthTokenResponse {
  access_token: string;
  token_type: string;
}

export async function loginWithEmail(email: string, password: string): Promise<AuthTokenResponse>;
export async function registerWithEmail(name: string, email: string, password: string): Promise<AuthTokenResponse>;
export async function buyCredits(credits?: number, amount?: number): Promise<{ url: string }>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor frontend API helper layer to typed, lint-clean request handling</name>
  <files>web/src/services/api.ts</files>
  <behavior>
    - API helpers return typed payloads and surface backend detail messages consistently.
    - Unauthorized responses route users to login through one shared path.
    - Header construction and fetch options avoid `as any` and pass eslint rules.
  </behavior>
  <action>Implement a shared request helper and typed error extraction in `web/src/services/api.ts` per D-02 and D-04. Remove `any` casts, add missing helper coverage for contact creation used by dashboard contacts flow, and preserve existing endpoint contracts per D-05.</action>
  <verify>
    <automated>Set-Location web; npm.cmd run lint -- src/services/api.ts</automated>
  </verify>
  <done>`web/src/services/api.ts` has no explicit-any lint errors and all exported API helpers compile with concrete response types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stabilize auth and checkout navigation behavior against environment origin drift</name>
  <files>web/src/app/auth/callback/page.tsx, web/src/app/login/page.tsx, web/src/app/signup/page.tsx, web/src/app/pricing/page.tsx, web/next.config.ts</files>
  <behavior>
    - Google auth start URLs resolve correctly in local and hosted environments.
    - OAuth callback stores token and lands users on dashboard without redirect loop.
    - Checkout flow redirects to Stripe URL on success and to login on auth failure.
  </behavior>
  <action>Refactor auth and pricing pages to use one origin-resolution utility and replace fragile redirect mutations with predictable navigation per D-04. Keep route and query contracts intact per D-05, and align local rewrite comments/config with deployment contract expectations.</action>
  <verify>
    <automated>Set-Location web; npm.cmd run lint -- src/app/auth/callback/page.tsx src/app/login/page.tsx src/app/signup/page.tsx src/app/pricing/page.tsx next.config.ts</automated>
  </verify>
  <done>Auth and checkout pages pass lint and route users through stable login/signup/callback/checkout transitions in both local and deployed environments.</done>
</task>

</tasks>

<verification>
Run frontend lint for touched auth/session files and manually spot-check login, signup, and pricing checkout transitions with backend running.
</verification>

<success_criteria>
- Frontend API/session boundary is lint-clean and type-safe.
- Login/signup/callback/pricing routes no longer depend on brittle redirect behavior.
- API contract compatibility with current backend remains unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-frontend-refinement-and-ship-readiness/02.1-01-SUMMARY.md`
</output>
